node_modules/
dist/
.env
*.db
*.log
*.tsbuildinfo
.obsidian/
.workbuddy/
.hermes/
obj\[c\]
packages/server/data/

# 打包工具（不进仓库，仅本地发布用）
dist-pkg/
pkg.config.json
scripts/
打包成品/

# Electron 桌面版构建产物（web/ 与 wasm 由 build 脚本组装，不进 git）
packages/desktop/web/
packages/desktop/sql-wasm.wasm

# 本机开发文档（仅本地，不随 git 同步）
开发说明.md
